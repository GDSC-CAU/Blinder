import 'package:flutter/material.dart';

class CropTestPage extends StatelessWidget {
  const CropTestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
